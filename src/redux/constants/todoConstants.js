// khai báo các action types

const PREFIX = "todo";

export const CHANGE_FILTER = `${PREFIX}/CHANGE_FILTER`;
export const CHANGE_SEARCH_VALUE = `${PREFIX}/CHANGE_SEARCH_VALUE`;
export const GET_TODOS = `${PREFIX}/GET_TODOS`;
